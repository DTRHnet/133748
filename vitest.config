import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    // Explicitly define where Vitest should look for test files
    dir: './test', // Look in the 'test' directory relative to this config file
    // You can also fine-tune the include/exclude patterns if needed
    // include: ['**/*.{test,spec}.?(c|m)[jt]s?(x)'],
    // exclude: ['**/node_modules/**', '**/dist/**', /* ... other patterns ... */],
  },
});
